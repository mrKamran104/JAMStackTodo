import "./Header.css"
import React from "react";

const Header = () => {
  return (
    <header className="app-header">
      <h2>Serverless JAMStack Todo app with Netlify, Gatsby, GraphQL, and FaunaDB</h2>
    </header>
  )
}

export default Header
